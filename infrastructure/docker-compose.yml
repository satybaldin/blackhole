services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: blackhole
      POSTGRES_USER: blackhole
      POSTGRES_PASSWORD: blackhole_dev_password
    ports:
      - "5432:5432"
    volumes:
      - blackhole_pg:/var/lib/postgresql/data

volumes:
  blackhole_pg:
